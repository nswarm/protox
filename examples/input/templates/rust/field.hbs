{{#if is_map}}
    {{field_name}}: ::std::collections::HashMap<{{relative_key_type}}, {{relative_value_type}}>,
{{else}}
    {{#if is_array}}
    {{field_name}}: Vec<{{relative_type}}>,
    {{else}}
    {{field_name}}: {{relative_type}},
    {{/if}}
{{/if}}
