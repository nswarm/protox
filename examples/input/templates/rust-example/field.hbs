{{#if is_map}}
    pub {{field_name}}: ::std::collections::HashMap<{{relative_key_type}}, {{relative_value_type}}>,
{{else}}
    {{#if is_array}}
    pub {{field_name}}: Vec<{{relative_type}}>,
    {{else}}
    pub {{field_name}}: {{relative_type}},
    {{/if}}
{{/if}}
