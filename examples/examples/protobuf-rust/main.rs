use examples::Result;

/// This example passes all arguments straight through to the protobuf compiler.
fn main() -> Result<()> {
    //     let _ = create_dir_all(output_path()?);
    //     let input = input_dir()?;
    //     let output = output_dir()?;
    //     let proto_paths = find_all_with_extension(input_path()?, ".proto")?;
    //     let mut args = vec!["--proto_path", &input, "--csharp_out", &output];
    //     for proto_path in &proto_paths {
    //         args.push(proto_path.to_str().unwrap());
    //     }
    //
    //     struct_ffi_gen(&args)?;
    //
    //     Ok(())
    // }

    Ok(())
}
